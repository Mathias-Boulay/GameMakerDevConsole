///__spse_calling_instance(String objectName);
/*Check if the calling instance the the one we want
  Mainly syntaxic sygar for myself.
  Developers should only use this with the template in the documentation
  */
return (object_get_name(object_index) == argument0);

